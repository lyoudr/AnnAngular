/* Component DOM testing */
// The class-only tests can tell you about class behavior.
// They cannot tell you if the component is going to render properly, respond
// You may also be able to get the service from the root injector via TestBed.get(). This easier to remember and less verbose. But it only works when Angular injects the 
import { RankpiecesPipe } from './rankpieces.pipe';

describe('RankpiecesPipe', () => {
  it('create an instance', () => {
    const pipe = new RankpiecesPipe();
    expect(pipe).toBeTruthy();
  });
});

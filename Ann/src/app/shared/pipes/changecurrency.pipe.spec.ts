import { ChangecurrencyPipe } from './changecurrency.pipe';

describe('ChangecurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangecurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
